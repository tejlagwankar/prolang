import 'package:flutter/material.dart';

class KotlinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kotlin'),
      ),
      body: Center(
        child: Text('This is the Kotlin Screen'),
      ),
    );
  }
}
