import 'package:flutter/material.dart';

class RubyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ruby'),
      ),
      body: Center(
        child: Text('This is the Ruby Screen'),
      ),
    );
  }
}
